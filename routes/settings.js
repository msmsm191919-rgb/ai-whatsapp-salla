const express = require('express');
const router = express.Router();

// All settings routes have been unified in app.js to prevent conflicts
// and ensure proper multi-number config and knowledge base save logic.
// This empty router allows falling through to the correct handlers in app.js.

module.exports = router;
